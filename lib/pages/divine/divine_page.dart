import 'package:flutter/material.dart';

class DivinePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Bói ra ma, quét nhà ra rác."),
    );
  }
}